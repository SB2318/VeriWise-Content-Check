import { FormatterType } from "./formatter";
export interface RunOptions {
    locale: string;
    format: FormatterType;
    globPatterns: string[];
}
export declare function run(options: RunOptions): Promise<string>;
