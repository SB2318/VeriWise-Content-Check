import { TextstatResult } from "@textstat/kernel";
export declare function collectDetails(result: TextstatResult, prefix: string): {
    fieldNames: string[];
    unwindFields: string[];
    details: any[];
};
export declare function format(results: TextstatResult[]): string;
