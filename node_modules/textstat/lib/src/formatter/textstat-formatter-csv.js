"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var lodash_uniq_1 = __importDefault(require("lodash.uniq"));
var json2csv_1 = require("json2csv");
var commonPathPrefix = require("common-path-prefix");
function collectDetails(result, prefix) {
    var fieldNames = [];
    var unwindFields = [];
    var details = [];
    result.messages.forEach(function (message) {
        message.data.details.forEach(function (item) {
            var _a;
            fieldNames.push(item.name);
            if (Array.isArray(item.value)) {
                unwindFields.push(item.name);
            }
            details.push((_a = {
                    filePath: result.filePath,
                    displayPath: result.filePath.replace(prefix, ""),
                    ruleId: message.ruleId,
                    message: message.message
                },
                _a[item.name] = item.value,
                _a));
        });
    });
    return {
        fieldNames: fieldNames,
        unwindFields: unwindFields,
        details: details
    };
}
exports.collectDetails = collectDetails;
function format(results) {
    var fieldNames = ["filePath", "displayPath", "ruleId", "message"];
    var unwindFields = [];
    var details = [];
    var allFilePathList = results.map(function (result) { return result.filePath; });
    var prefix = commonPathPrefix(allFilePathList);
    results.forEach(function (result) {
        var info = collectDetails(result, prefix);
        fieldNames = lodash_uniq_1.default(fieldNames.concat(info.fieldNames));
        unwindFields = lodash_uniq_1.default(unwindFields.concat(info.unwindFields));
        details = details.concat(info.details);
    });
    var parser = new json2csv_1.Parser({
        fields: fieldNames,
        unwind: unwindFields
    });
    return parser.parse(details);
}
exports.format = format;
//# sourceMappingURL=textstat-formatter-csv.js.map