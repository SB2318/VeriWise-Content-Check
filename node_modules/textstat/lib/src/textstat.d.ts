export interface ReportOptions {
    locale: string;
    globPatterns: string[];
}
export declare function report(options: ReportOptions): Promise<import("@textstat/kernel/lib/src/type").TextstatResult[]>;
