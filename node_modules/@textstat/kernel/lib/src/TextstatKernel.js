"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
var kernel_1 = require("@textlint/kernel");
var path = __importStar(require("path"));
var ResultValidator_1 = require("./ResultValidator");
function bindTrailingArgs(fn, sharedDeps) {
    return function (context, options) {
        if (typeof fn.default === "function") {
            return fn.default(context, options, sharedDeps);
        }
        else {
            return fn(context, options, sharedDeps);
        }
    };
}
exports.createParser = function (plugins) {
    if (plugins === void 0) { plugins = []; }
    var textlintPluginDescriptors = plugins.map(function (plugin) {
        return new kernel_1.TextlintPluginDescriptor(plugin);
    });
    var pluginDescriptors = new kernel_1.TextlintPluginDescriptors(textlintPluginDescriptors);
    return {
        parse: function (text, filePath) {
            var ext = path.extname(filePath);
            var plugin = pluginDescriptors.findPluginDescriptorWithExt(ext);
            if (!plugin) {
                throw new Error("Anyone does not support ext" + ext);
            }
            return plugin.processor.processor(ext).preProcess(text, filePath);
        }
    };
};
// @ts-ignore
function isRulePresetFactory(v) {
    return typeof v === "object" && v.createPreset === "function";
}
// @ts-ignore
function createTextstatPresetToTextlintPreset(preset, sharedDependencies) {
    var rules = Object.keys(preset.rules).map(function (key) {
        var rule = preset.rules[key];
        return {
            ruleId: rule.ruleId,
            rule: bindTrailingArgs(rule.rule.report, sharedDependencies),
            options: rule.options
        };
    });
    return {
        rules: rules,
        rulesConfig: preset.rulesConfig
    };
}
var TextstatKernel = /** @class */ (function () {
    function TextstatKernel() {
    }
    TextstatKernel.prototype.report = function (text, options) {
        var kernel = new kernel_1.TextlintKernel();
        var parserDeps = exports.createParser(options.plugins);
        var sharedDependencies = {
            locale: options.sharedDeps.locale,
            filePathList: options.sharedDeps.filePathList,
            parser: parserDeps
        };
        return kernel
            .lintText(text, {
            ext: options.ext,
            filePath: options.filePath,
            // Wrap with (context, options, deps) => {}
            rules: options.rules &&
                options.rules.map(function (rule) {
                    return {
                        ruleId: rule.ruleId,
                        rule: bindTrailingArgs(rule.rule.report, sharedDependencies),
                        options: rule.options
                    };
                }),
            filterRules: options.filterRules &&
                options.filterRules.map(function (rule) {
                    return {
                        ruleId: rule.ruleId,
                        rule: bindTrailingArgs(rule.rule.report, sharedDependencies),
                        options: rule.options
                    };
                }),
            plugins: options.plugins
        })
            .then(function (result) {
            if (!ResultValidator_1.validateResult(result)) {
                throw new Error("result is not compatible TextstatResult");
            }
            return result;
        });
    };
    return TextstatKernel;
}());
exports.TextstatKernel = TextstatKernel;
//# sourceMappingURL=TextstatKernel.js.map