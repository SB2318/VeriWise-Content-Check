import { TextlintKernelPlugin } from "@textlint/kernel";
import { TextstatKernelFilterRule, TextstatKernelRule } from "@textstat/rule-context";
import { LocaleTag } from "@textstat/rule-context";
import { TextstatResult } from "./type";
export declare const createParser: (plugins?: TextlintKernelPlugin[]) => {
    parse(text: string, filePath: string): import("@textlint/ast-node-types").TxtNode;
};
export declare class TextstatKernel {
    report(text: string, options: {
        ext: string;
        filePath?: string;
        plugins?: TextlintKernelPlugin[];
        rules?: TextstatKernelRule[];
        filterRules?: TextstatKernelFilterRule[];
        configBaseDir?: string;
        sharedDeps: {
            locale?: LocaleTag;
            filePathList: string[];
        };
    }): Promise<TextstatResult>;
}
