export declare type LocaleTag = "en" | "cs" | "de" | "es" | "fr" | "it" | "ja" | "ko" | "pl" | "ru" | "tr" | "zh-CN" | "zh-TW" | string;
declare type LocalizeMessageMulti = {
    [P in LocaleTag]?: string;
} & {
    en: string;
};
export declare type LocalizeMessages = {
    [index: string]: string | LocalizeMessageMulti;
};
export declare class Localize<T extends LocalizeMessages> {
    private messages;
    private locale;
    constructor(messages: T, locale?: LocaleTag);
    /**
     * Translate the `key` to text
     * If the `key` includes some variable, pass option and assign it to the variable.
     * @param key
     * @param options
     */
    t: (key: keyof T, options?: object | undefined) => string;
    private matchLocale;
    private applyOption;
}
export {};
