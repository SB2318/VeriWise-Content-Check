"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
var rule_context_1 = require("@textstat/rule-context");
var fs = __importStar(require("fs"));
var path = __importStar(require("path"));
var isUrl = require("is-url");
var visit = require("unist-util-visit");
var stripHash = function (pathString) {
    // remove #
    return pathString.replace(/#.*$/, "");
};
exports.meta = {
    docs: {
        homepage: "",
        description: ""
    },
    messages: {
        message: {
            en: "Dependency links in the document",
            ja: "ドキュメントの依存関係"
        },
        "To Links": {
            en: "To Links",
            ja: "リンク先"
        },
        "From Links": {
            en: "From Links",
            ja: "リンク元"
        }
    }
};
var cacheMap = new Map();
exports.report = function (context, _options, deps) {
    var _a;
    var Syntax = context.Syntax, report = context.report, getFilePath = context.getFilePath;
    var t = new rule_context_1.Localize(exports.meta.messages, deps.locale).t;
    var toLinks = [];
    var fromLinks = [];
    var currentFilePath = getFilePath();
    var currentDocumentExt = currentFilePath ? path.extname(currentFilePath) : "";
    var isDocumentType = function (filePath) {
        return currentDocumentExt === path.extname(filePath);
    };
    /**
     * Add "From to the document" link
     * @param baseFilePath
     * @param urlString
     */
    var addFromLink = function (baseFilePath, urlString) {
        if (!urlString) {
            return;
        }
        if (isUrl(urlString)) {
            return;
        }
        var absoluteToUrl = path.resolve(path.dirname(baseFilePath), stripHash(urlString));
        if (currentFilePath !== absoluteToUrl) {
            return;
        }
        fromLinks.push({
            type: isDocumentType(baseFilePath) ? "document" : "file",
            path: baseFilePath
        });
    };
    var getLinkFromFilePath = function (fromFilePath) {
        if (cacheMap.has(fromFilePath)) {
            return cacheMap.get(fromFilePath);
        }
        var text = fs.readFileSync(fromFilePath, "utf-8");
        var AST = deps.parser.parse(text, fromFilePath);
        var urls = [];
        visit(AST, [Syntax.Link, "Definition"], function (node) {
            if (!(node.identifier && /^\^/.test(node.identifier))) {
                urls.push(node.url);
            }
        });
        cacheMap.set(fromFilePath, urls);
        return urls;
    };
    var fromLinkHandle = function (fromFilePath) {
        getLinkFromFilePath(fromFilePath).forEach(function (url) {
            addFromLink(fromFilePath, url);
        });
    };
    deps.filePathList.forEach(fromLinkHandle);
    /**
     * Add "The document to" link
     * @param urlString
     */
    var addToLink = function (urlString) {
        if (!urlString) {
            return;
        }
        if (isUrl(urlString)) {
            toLinks.push({
                type: "url",
                path: urlString
            });
        }
        else if (currentFilePath) {
            toLinks.push({
                type: isDocumentType(stripHash(urlString)) ? "document" : "file",
                path: path.resolve(path.dirname(currentFilePath), stripHash(urlString))
            });
        }
    };
    return _a = {},
        _a[Syntax.Link] = function (node) {
            addToLink(node.url);
        },
        _a["Definition"] = function (node) {
            addToLink(node.url);
        },
        _a[Syntax.Document + ":exit"] = function (node) {
            report(node, {
                message: t("message"),
                range: node.range,
                details: [
                    {
                        name: t("To Links"),
                        value: toLinks
                    },
                    {
                        name: t("From Links"),
                        value: fromLinks
                    }
                ]
            });
        },
        _a;
};
//# sourceMappingURL=index.js.map